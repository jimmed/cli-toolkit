// @flow
import { type CommandDefinition } from './types.flow';

const argPrompt = async (command: CommandDefinition<*>, args: *) => {
  throw new Error('TODO: argPrompt is not implemented');
};
export default argPrompt;
