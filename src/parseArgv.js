// @flow
import { type CommandDefinition } from './types.flow';

const parseArgv = (commands: Array<CommandDefinition<*>>, argv: *) => {
  throw new Error('TODO: parseArgv is not implemented');
};
export default parseArgv;
